type Query {
  categories: [Category]
  transactions: [Transaction]
}

type Mutation {
  addCategory(name: String, parent: ID): Category
}

type Category {
  id: ID!
  name: String
  parent: ID
}

type Transaction {
  id: ID!
  detail: String
  value: Int
  date: String    
  categories: [Category]
}